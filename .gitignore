# Brainstorm companion (local mockups/state)
.superpowers/

# OS
.DS_Store
Thumbs.db

# Node / JS
node_modules/
npm-debug.log*
yarn-error.log*
*.log

# Expo / React Native (generated)
.expo/
.expo-shared/
dist/
web-build/
# Native projects are generated by `expo prebuild` via config plugins
/ios/
/android/
# Local build artifacts — `eas build --local` output and version-named copies of it
*.apk
*.aab
*.ipa
*.apk.zip
*.ipa.zip
# Gradle output from compiling the local Expo module
modules/schedularm-alarm/android/build/

# Env / secrets
.env
.env.*
!.env.example

# Apple App Store Connect API key — NEVER commit the private key
/credentials/
*.p8

# Editor
.idea/
*.swp
.vscode/*
!.vscode/extensions.json
