// Also registered as the privacy-policy URL in both store consoles — the
// stores additionally require it to be reachable from INSIDE the app
// (Apple 5.1.1(i), Play User Data policy), and the consent surfaces must
// offer it at the moment of choice (informed-consent basis).
export const PRIVACY_POLICY_URL = 'https://2umean.github.io/miri-alarm/privacy.html';
