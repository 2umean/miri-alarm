<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <!--
    Components (service / activity / receivers) and permissions are injected into
    the *app* manifest by plugin/withSchedularmAlarm.js so the lock-screen / FGS
    flags land in the merged manifest exactly as aggressive OEMs require.
  -->
</manifest>
